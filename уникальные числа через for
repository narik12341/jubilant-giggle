numbers= [1,2,3,3,4,5,6]
def unique_num(numbers):
    unique = []

    for num in numbers:
        if num in unique:
            continue
        else:
            unique.append(num)
            return unique
print(unique_num(numbers))
